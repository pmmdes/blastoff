//Faça um algoritmo para apresentar se dois números são múltiplos

const a = 34;
const b = 2;

if (a > b) {
    a%b == 0 ? console.log("São múltiplos.") : console.log("Não são múltiplos.") 
} 
else {
    b%a == 0 ? console.log("São múltiplos.") : console.log("Não são múltiplos.") 
}